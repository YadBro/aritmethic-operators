/**
 * @param {number} num1  add number 1
 * @param {number} num2 add number 2
 * @returns Returns result your subtraction
 * @example
 * subtraction(1, 1);
 * // => 0
 */

function subtraction(num1: number, num2: number) {
  return num1 - num2;
}

export {subtraction}