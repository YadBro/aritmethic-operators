/**
 * @param {number} num1  add number 1
 * @param {number} num2 add number 2
 * @returns Returns result your addition
 * @example
 * addition(1, 1);
 * // => 2
 */

function addition(num1: number, num2: number) {
  return num1 + num2;
}

export {addition}